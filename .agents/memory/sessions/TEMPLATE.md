# YYYY-MM-DD | <agent> | <product> | <task>

Copy to `YYYY-MM-DD-<agent>-<product>--<task>.md` (<task> = the feature id when there is one; `workspace` is the product of a session on `main`). At most 40 lines. The journey only — facts go into `docs/<product>/`.

## Task

## Done

## Decisions & pitfalls
- Why this option; failed attempts and why they failed.

## TODO
- `[workspace]` <a defect in the workspace, worded without the organization's names — on an organization's branch>
